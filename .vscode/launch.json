{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",                 // or "chrome"
      "request": "launch",
      "name": "Open current file in browser",
      "file": "${file}"               // open this file instead of localhost
    }
  ]
}